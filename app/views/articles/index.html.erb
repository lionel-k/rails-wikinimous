<div class="container">
  <% @articles.each do |article| %>
  <div class="row">
        <div class="col-sm-8 col-sm-offset-2">
      <h2><%= link_to article.title, article_path(article) %></h2>
      <p><%=  markdown("#{article.content[0..30]}") %></p>
    </div>
  </div>
  <% end %>
</div>


